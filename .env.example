# Copy this file to .env.local and fill in values before running the app.
# Do NOT commit your real secrets. These are example placeholders only.

# NextAuth / Session
AUTH_SECRET=replace-with-a-long-random-secret
AUTH_GOOGLE_ID=your-google-oauth-client-id
AUTH_GOOGLE_SECRET=your-google-oauth-client-secret

# Google Analytics (optional: service account JSON string or PEM)
# If you use a JSON service account key, paste the full JSON value (escaped/newlines ok).
# If you use a PEM private key, set GOOGLE_ANALYTICS_SERVICE_ACCOUNT_KEY to the PEM string
# and set GOOGLE_SERVICE_ACCOUNT_EMAIL to the service account email.
GOOGLE_ANALYTICS_SERVICE_ACCOUNT_KEY=your-service-account-json-or-pem
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account-email@project.iam.gserviceaccount.com
GOOGLE_CLOUD_PROJECT_ID=your-google-project-id
GOOGLE_ANALYTICS_PROPERTY_ID=your-ga4-property-id
GOOGLE_ANALYTICS_ACCOUNT_ID=your-ga4-account-id

# Gemini (Generative AI) API key (optional)
GEMINI_API_KEY=your-gemini-api-key

# Runtime
NODE_ENV=development

# Example client-side public variable (optional)
# NEXT_PUBLIC_SOME_CONFIG=some-value
